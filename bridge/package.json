{
  "name": "just-dance-remote-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "LAN bridge for the Just Dance request bot Android companion app.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}

